{
  "name": "honey",
  "version": "1.0.0",
  "description": "A website to sell honey",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "node-sass --watch *scss -o css",
    "browser-sync": "browser-sync start --server --files \"index.html\" \"./css/*.css\"",
    "dev": "concurrently \"npm run scss\" \"npm run browser-sync\""
  },
  "author": "Paul Petillot",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.27.10",
    "concurrently": "^5.2.0",
    "node-sass": "^4.14.1"
  }
}
